/*
import { Container } from "typedi";
import { IVerificationRepository } from "@domain/repositories/impl.verification.repository";

let VerificationRepository = Container.get<IVerificationRepository>('verificationRepository');

async function generateVerificationCode(email: string) {
    let verificationRecord = VerificationRepository.findByEmail(email);
}
*/